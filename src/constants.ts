const _baseUrl: string = 'https://auth-express-ts.up.railway.app/api';

export const loginUrl = `${_baseUrl}/user/login`;

export const signupUrl = `${_baseUrl}/user/register`;

export const contentUrl = `${_baseUrl}/private/content`;

export const resetPasswordUrl = `${_baseUrl}/user/reset-password`;

export const updatePasswordUrl = `${_baseUrl}/user/update-password`;
